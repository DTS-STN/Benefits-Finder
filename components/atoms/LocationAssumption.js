import React from "react";

//Location assumption banner(?) that needs a button in the future to confirm
export default function LocationAssumption(props) {
  return (
    <div>
      <h6 id={props.id}></h6>
    </div>
  );
}
